function startJobSearch() {
    var jobTitle = document.getElementById('jobTitle').value;

    // Add logic here to send the job title to the backend for processing
    // For simplicity, I'm just adding log the job title for now
    console.log('Searching for jobs with title:', jobTitle);
}
